{
  "configurations": [
    {
      "type": "chrome",
      "name": "Launch Chrome",
      "request": "launch",
      "url": "http://localhost:4221",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
